#include <stdio.h>

/**
 * main - Prints alll possible combinations of two two_digit numbers,
 *	ranging from 0-99, separated by a comma followed by a space.
 *
 * Return: Always 0.
 */
int main(void)
{
	int a, b, c, d;


	return (0);
}
